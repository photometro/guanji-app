# 实施记录 — v3.22（#138/#140/#141）发生前状况多选与屏幕方向稳定

日期：2026-08-18

## 实施条目

- 本次 v3.22 实施范围：#138 / #140 / #141。
- #138 “发生前情况”支持多选：直接记录、补记、编辑和计时汇总使用 `observations[]`，支持取消选择；“无特别情况”与其他标签互斥；保留 `observation` 首项别名兼容旧数据。
- #140 App 未锁定屏幕方向：Android `MainActivity` 固定为 `portrait`，避免系统自动旋转导致横竖屏切换。
- #141 记录页标题简化：普通详情页和计时汇总页统一显示“发生前状况”，移除括号说明。

## 改动文件

- `www/index.html`：更新两个记录页标题；所有 CSS/JS 缓存参数 `v=111` → `v=112`；版本显示 v3.22。
- `www/js/storage.js`：新增多选数组归一化、旧单值/旧情绪诱因适配、历史 `media` 语义和多标签展示。
- `www/js/ui-sheet.js`：多选 chips、互斥标签、自定义项即时刷新、编辑回填、多选 CSV 导出。
- `www/js/records.js`、`www/js/ui-timer.js`：直接记录、快速记录和计时汇总保存数组与兼容别名。
- `www/js/secure.js`：CSV 新增 `发生前状况(多选)` 字段，逐项 URL 编码后用 `|` 分隔，并兼容旧列；加密备份结构原样保留。
- `www/js/ai.js`：按“出现于记录比例”聚合并明确标签可共存；报告指纹覆盖多选数组。
- `android/app/src/main/AndroidManifest.xml`：`MainActivity` 增加 `android:screenOrientation="portrait"`。
- `android/app/build.gradle`：versionCode 42 → 43，versionName 3.21 → 3.22。
- `CHANGELOG.md`、`README.md`、`IMPROVEMENTS.md`：补充 v3.22 说明、实施记录和条目状态。

## 验证结果

- JS 语法：`storage.js`、`records.js`、`secure.js`、`ai.js`、`ui-sheet.js`、`ui-timer.js`、`app.js` 全部 `node --check` 通过；`git diff --check` 无错误。
- 浏览器：`debug/verify-138-browser.cjs` 通过（多选、互斥、旧记录、CSV、AI payload、两个“发生前状况”标题）；`verify-132-browser.cjs`、`verify-139-browser.cjs` 通过。
- Android：`npx cap sync android` 通过；`gradlew.bat assembleDebug --no-daemon` 通过。
- APK：`android/app/build/outputs/apk/debug/app-debug.apk`，versionCode 43 / versionName 3.22；合并 Manifest 确认 `screenOrientation="portrait"`。
- 真机：461QYFDN226NF 安装并通过 `debug/verify-138-device.cjs`；直接记录与计时汇总均保存多选数组，成人内容影响正确推导 `media=true`，显示 v3.22；`verify-139-device.cjs` 的底部动作区与滚动回归通过。
- 方向锁：临时将设备旋转策略设为 90° 后仍报告 `mCurrentRotation=ROTATION_0`，测试结束已恢复 `accelerometer_rotation=1`、`user_rotation=0`。
- 截图：`screenshots/guanji-v322-138-home.png`。

## 待确认

- 首版多选不限制数量，以减少记录时的额外阻塞；后续可根据实际使用数据再评估是否需要上限。
- `发生前状况(多选)` 的稳定字段使用逐项编码的 `|` 分隔；旧 CSV 和加密备份保持兼容。

