# 实施记录 — v3.31 #159 个人洞察 AI-first 双态卡片重构

日期：2026-08-19

## 实施条目

- #159 个人洞察 AI-first 双态卡片重构：将 #151–#158 统一为双态六卡片；无 AI 时完整显示本地事实，AI 可用时以 AI 主观察为先、本地数据为证据。

## 改动文件

- `www/js/ai.js`：新增 AI / 本地状态渲染层级；重做事实→AI 顺序、共同出现中点表达、单一变化比较、AI 优先建议列表；补充近 7 天提示词边界和旧报告状态入口。
- `www/css/screens.css`：增加 AI 主叙事、证据行、组合表达、变化比较、建议来源和旧版主按钮样式，保留既有 report-card 材质。
- `www/index.html`：移除可见窗口徽章，更新副标题、AI 动作说明、资源缓存参数和版本文案。
- `android/app/build.gradle`：versionCode 51 → 52，versionName 3.30 → 3.31。
- `CHANGELOG.md`：新增 v3.31 更新记录。
- `IMPROVEMENTS.md`：#159 及 #151–#158 标记为实施完成。
- `debug/verify-159-browser.cjs`：新增无 AI / AI、窄屏、隐私 payload、组合、变化和建议列表浏览器回归。

## 验证结果

- `node --check www/js/ai.js`：通过。
- `node debug/verify-159-browser.cjs`：通过；覆盖无 AI 本地模式、AI 主观察内联、近 7 天文案、单一变化比较、中点组合、建议最多 3 条、390px 无横向溢出、聚合 payload 隐私边界。
- Android 静态资源：已将 `www/` 同步到 `android/app/src/main/assets/public`，并核对 `index.html` 与 `ai.js` 哈希一致。
- `gradlew.bat assembleDebug --no-daemon`：通过，生成 `android/app/build/outputs/apk/debug/app-debug.apk`（SHA-256：`0198C224895AD9240F1949993608C94782F6EB6F673C267269FA1A5F2B08845B`）。
- 真机：已恢复 adb 配置目录可写，设备 `461QYFDN226NF`（MEIZU 21）在线；v3.31 APK 安装成功，包版本 `versionCode=52 / versionName=3.31`。
- 真机 WebView CDP：通过 `adb forward tcp:9223` 连接成功；个人洞察页显示 6 张卡片，AI 模式能在真实数据上生成并回填 6 张卡片，页面级仅保留 1 个“重新生成观察”入口；无可见窗口徽章、无重复图表，控制台无异常。
- 真机视觉截图：`screenshots/guanji-v331-insight-local.png`、`screenshots/guanji-v331-insight-ai.png`。
- 真机发现（待修复）：AI 观察来源标签当前计算样式为 `display:flex`，在真机截图中与观察正文分两行；原因是旧版 `.insight-card-ai .insight-ai-source` 选择器优先级覆盖了 #159 的 inline 规则，本轮仅记录，不改动代码。

## 待确认

- 需要修复 AI 观察来源标签的行内布局，再补做一次真机截图回归；其余 v3.31 真机安装、CDP、AI/本地结构验证已完成。

