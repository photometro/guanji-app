# 实施记录 — v3.25 发生前状况扩展与详情页布局修复

日期：2026-08-19

## 实施条目

- #144 “发生前状况”扩展至 15 项：新增低落、烦躁、疲惫、顺手就做了、想放松一下；保持统一稳定 key、多选和中性自报语义。
- #145 取消渐进式收纳后的详情页布局修复：补记详情页内容不足时自适应收缩，内容过多时滚动，底部操作区保持稳定；折叠版与平铺版复用同一组选项。

## 改动文件

- `www/js/storage.js`：新增 5 个稳定 key、维度元数据和旧/研究候选文案 alias。
- `www/css/sheets.css`：详情 sheet 从固定 86% 高度改为内容自适应 + 溢出滚动。
- `www/js/ui-sheet.js`：新增详情内容溢出状态更新，覆盖打开、标签变化、窗口尺寸变化和高度过渡结束。
- `www/index.html`：版本文案与资源缓存参数升级至 `v=115`。
- `android/app/build.gradle`：升级至 `versionCode 46`、`versionName 3.25`。

## 验证结果

- `node --check`：storage、ui-sheet、records、ui-timer、ai 全部通过。
- 浏览器回归：390×844 视口下 15 项顺序、多选/独占、旧文案兼容、短内容约 20px 间距、大量选项滚动、计时汇总和控制台检查通过。
- APK：`npx cap sync android` 与 `gradlew.bat assembleDebug --no-daemon` 成功。
- 真机：魅族 21（461QYFDN226NF）安装并启动 v3.25；WebView CDP 触摸、多选/独占、短内容布局、长内容滚动、计时汇总和控制台检查通过。
- 截图：`screenshots/guanji-v3.25-144-145.png`。

## 待确认

- 新增 5 个文案已按当前推荐方案落地，后续可根据真实使用反馈再调整命名或排序。

