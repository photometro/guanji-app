# 实施记录 — v3.28 个人洞察回归旧版 AI 卡片与自动触发

日期：2026-08-19

## 实施条目

- #147 个人洞察回归旧版 AI 卡片与自动触发逻辑：六个洞察主题全部使用旧版 `report-card` 视觉并完整展开；未生成 AI 时显示轻量事实提示和卡片内 CTA；一次统一 AI 请求填充六张卡片；恢复进入洞察页自动检查、页面可见时记录变更 1.5 秒防抖刷新、页面不可见时下次进入刷新。

## 改动文件

- `www/index.html`：个人洞察页改为 `insightCards` 六卡片挂载点；保留隐藏兼容入口；更新页面副标题、v3.28 和资源缓存参数。
- `www/js/ai.js`：重写六卡片本地/AI 状态渲染；增加卡片内配置/生成/重试入口；恢复统一请求、指纹过期、进入页面自动生成和记录变更防抖；保留聚合 payload 与 AI 边界。
- `www/js/app.js`：切换到洞察页时调用 `maybeAutoGenerate()`。
- `www/js/records.js`：记录变更钩子同步触发洞察防抖刷新。
- `www/css/screens.css`：新增旧版 AI 卡片、卡片内 AI 观察、加载和 CTA 样式。
- `android/app/build.gradle`：versionCode 49、versionName 3.28。
- `CHANGELOG.md`、`README.md`、`IMPROVEMENTS.md`：同步 #147、v3.28、自动触发与隐私说明。
- `debug/verify-147-browser.cjs`、`debug/verify-147-device.cjs`：新增浏览器与真机 CDP 回归脚本。

## 本轮界面修订

- 六张卡片继续共享旧版 `report-card` 材质，但按内容分别改为数字概览、时间带、状况标签、共同出现关系、变化对比和编号建议列表。
- AI 操作收敛到总览卡片的单一入口；未配置 AI 时不再让六张卡片重复显示“重新生成/配置”按钮。
- 前端缓存参数更新至 `v=119`，保留 v3.28 的数据与 AI 隐私边界。

## 验证结果

- `node --check www/js/ai.js`：通过。
- `node --check www/js/app.js`：通过。
- `node --check www/js/records.js`：通过。
- 浏览器回归：六张卡片、固定近 7 天、无 Key 零请求、一次请求填充六卡、AI payload 不含备注/自定义标签、可见页防抖刷新、隐藏页下次进入刷新均通过；无控制台错误。
- APK：`assembleDebug --no-daemon` 构建成功；产物 `android/app/build/outputs/apk/debug/app-debug.apk`。
- 真机：设备 `461QYFDN226NF` 安装 v3.28 成功；CDP 验证六卡、固定近 7 天、无 Key 零请求、统一一次请求填充六卡、隐私 payload 和控制台错误均通过。
- 真机截图：`screenshots/guanji-v328-147-insight-final.png`（初始回归截图保留为 `screenshots/guanji-v328-147-insight.png`）。

## 待确认

- 请在日常真机数据下观察六张卡片的滚动长度和 AI 自动刷新时机；若自动观察频率过高，再调整触发门槛或缓存策略。

