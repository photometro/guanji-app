# 实施记录 — v3.32 #160 AI 免责声明与重新生成分析入口

日期：2026-08-19  
条目：#160

## 实施内容

- 恢复洞悉页页面级 AI 免责声明，统一说明数据来源为近 7 天脱敏聚合统计，并明确不上传单条记录、备注或附件。
- 免责声明覆盖 AI 已生成和 `ready-stale`（数据更新后待重新生成）两种状态，避免更新后消失。
- 页面级生成入口保留在六张洞察卡片下方，不在卡片内部重复生成按钮。
- 已配置 AI 时，按钮文案固定为“重新生成分析”，按钮不包含加号或 SVG 图标；未配置 AI 时仍显示“配置 AI”。
- 资源版本升级为 v=124，Android versionName 为 3.32、versionCode 为 53。

## 改动文件

- `www/js/ai.js`：新增免责声明渲染逻辑；调整页面级入口文案并移除按钮图标。
- `www/index.html`：加入页面级免责声明节点；资源缓存参数升级至 v=124；关于页版本号更新。
- `www/css/screens.css`：调整入口容器布局，使免责声明位于入口下方并保持旧版主按钮比例。
- `android/app/build.gradle`：版本号升级至 3.32（53）。
- `CHANGELOG.md`、`IMPROVEMENTS.md`：补充 v3.32/#160 变更与验收记录。
- `debug/verify-159-browser.cjs`、`debug/verify-159-device.cjs`：增加 #160 入口、图标、免责声明和样式断言。

## 验证结果

- `node --check www/js/ai.js`：通过。
- `node debug/verify-159-browser.cjs`：通过；本地态无免责声明，AI 态仅一个免责声明节点，按钮为“重新生成分析”且无 SVG，脱敏聚合请求未包含私密文本，`ready-stale` 仍显示免责声明。
- `npx cap sync android`：受当前 Node 环境 `uv_os_get_passwd returned ENOMEM` 影响未完成；已将 `www` 手动同步至 Android assets，并核对 `index.html`、`js/ai.js` 内容哈希一致。
- `gradlew.bat assembleDebug --no-daemon`：通过，生成 debug APK。
- 真机 MEIZU 21（`461QYFDN226NF`）安装并启动通过；`versionName=3.32`、`versionCode=53`。
- 真机 CDP 检查通过：六张卡片、页面级唯一入口、AI 态按钮“重新生成分析”、无 SVG 图标、免责声明仅出现一次且内容正确，洞察页无横向溢出。
- 真机截图：`screenshots/guanji-v332-insight-ai.png`。

## 验收结论

本条目已完成。用户确认的“按钮内只写重新生成分析、不加加号”已落实；未引入新的待确认项。

