# 实施记录 — v3.21 计时汇总页底部动作区固定

- 日期：2026-08-18
- 条目：#139【Bug】计时记录汇总页“存为记录”按钮位置上移
- 版本：versionName `3.21`，versionCode `42`

## 本次交付

计时结束汇总页拆分为“上方可滚动内容区 + 底部固定动作区”。标题、时长、开始时间和“发生前情况”放入 `.summary-scroll`；“存为记录／继续计时／放弃”保留在 `.summary-actions`，不随上方滚动移动。未使用 `position: fixed`，继续使用 flex 和安全区间距适配 WebView。

## 改动文件

- `www/index.html`：为 `#timerSummaryView` 增加 `.summary-scroll` 包裹上方内容，动作区留在滚动容器外。
- `www/css/sheets.css`：新增汇总滚动层的 `flex: 1`、`min-height: 0`、`overflow-y: auto`、触摸滚动；动作区改为 `flex: 0 0 auto` 并设置底部自适应间距。
- `www/js/ui-sheet.js`：事件绑定和保存逻辑保持不变。
- `android/app/build.gradle`：versionCode 41→42，versionName 3.20→3.21。
- `CHANGELOG.md`、`README.md`、`IMPROVEMENTS.md`：同步 v3.21 说明与 #139 实施状态。
- `debug/verify-139-browser.cjs`、`debug/verify-139-device.cjs`：新增大量标签、滚动稳定性、保存和版本回归脚本。

## 验证结果

- JS 语法检查与 `git diff --check` 通过。
- 浏览器回归通过：`scrollHeight=1276`、`clientHeight=579`，确认内容可滚动；滚动前后动作区 `topDelta=0`；保存记录得到 `adult_content_effect` / `media=true`；无控制台错误。
- Android：`npx cap sync android` 与 `gradlew.bat assembleDebug --no-daemon` 通过。
- 真机：魅族 `461QYFDN226NF` 安装 v3.21 APK 后 CDP 回归通过：`scrollHeight=1319`、`clientHeight=519`、动作区滚动前后 `topDelta=0`；保存触摸、版本显示和数据恢复均通过。
- APK：`android/app/build/outputs/apk/debug/app-debug.apk`。
- 真机截图：[guanji-v321-139.png](C:/Users/43124/Desktop/test/guanji-app/screenshots/guanji-v321-139.png)。

## 后续观察

继续观察真实设备上不同字体大小、横屏和 #138 多选标签实施后的滚动体验；当前保存、继续计时和放弃的业务逻辑未改变。

