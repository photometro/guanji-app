# 实施记录 — v3.29 个人洞察卡片视觉回归旧版

日期：2026-08-19

## 实施条目

- #148 个人洞察卡片视觉回归旧版，与内容编排分离：保留六类卡片内容结构和总览大字号；移除彩色顶部色带、渐变、光晕、主题色和 hover 上移；复用既有 `report-card` 与 `glass.css` 材质；AI 观察恢复轻量分隔线。

## 改动文件

- `www/css/screens.css`：洞察卡片外层回归 `var(--card)`、`var(--shadow-2)` 和旧版圆角/间距；内部内容的强调色统一使用现有主题变量；AI 观察改为轻量分隔线。
- `www/index.html`：关于页更新为 v3.29，资源缓存参数更新至 `v=120`。
- `android/app/build.gradle`：versionCode 50、versionName 3.29。
- `CHANGELOG.md`、`IMPROVEMENTS.md`：同步 #148 实施状态、用户确认和版本记录。
- `debug/verify-147-browser.cjs`、`debug/verify-147-device.cjs`：增加卡片与普通 `.card` 的 computed 材质一致性检查，并保留六类内容/单一 AI 入口回归。

## 验证结果

- JavaScript 语法检查：通过。
- 浏览器回归：六张卡片、六种内容编排、总览大字号、单一 AI 入口、经典模式无专属色带/光晕、与普通卡片背景/圆角/阴影一致，全部通过；无控制台错误。
- `npx cap sync android`：因本机 Node `uv_os_get_passwd returned ENOMEM` 失败；已使用等价资源同步方式更新 Android assets。
- APK：`assembleDebug --no-daemon` 构建成功，产物为 `android/app/build/outputs/apk/debug/app-debug.apk`。
- 真机：v3.29 APK 已安装到 `461QYFDN226NF`；设备当前处于锁屏/通知遮罩，WebView CDP 暂无法连接，需解锁后补做最终真机视觉回归。

## 待确认

- 解锁日常使用手机后，补做一次真机洞察页截图和 computed 材质一致性检查；不涉及数据结构或 AI 请求变更。

