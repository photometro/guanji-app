# 实施记录 — v3.26 #133 阶段 B、C

日期：2026-08-19  
条目：#133 个人洞察增强（阶段 B：本地事实层；阶段 C：AI 解读融合）  
版本：v3.26 / Android versionCode 47 / Web 资源 `v=116`

## 实施内容

### 阶段 B：个人洞察事实层

- 在现有 AI 分析页原地升级为“洞察 / 个人洞察”，没有新增第二个分析页面。
- 新增统一 `buildInsightSnapshot({ days, now })`，统一输出记录数、覆盖天数、填写覆盖率、时段桶、默认标签出现比例、共同出现组合、前后窗口变化和数据清晰度阶段。
- 个人洞察进入页面即可显示本地事实，支持 `7 天 / 30 天`窗口切换；首页原有“时段分布”完整保留。
- 数据清晰度仅描述样本是否足以形成轮廓，不评价用户状态、频率或行为好坏。

### 阶段 C：同页 AI 解读融合

- 复用本地 snapshot 构造聚合 payload；云端只接收默认标签的聚合 key/标签、次数、比例、时段桶、共同出现和窗口变化等统计信息。
- 默认不发送自定义标签原文、备注、附件、单条记录或完整时间线；用户明确点击后才发起 AI 请求。
- AI 只负责摘要、变化解释和温和建议；页面数字由本地 snapshot 作为唯一来源，不采用 AI 返回的统计比例。
- 保留轻量 `AI 生成` 来源标识、无 key/断网时的本地降级和追问能力；窗口切换不会静默生成云端报告。
- 更新 AI 系统边界：禁止露骨内容、诊断、羞耻化、因果化和虚构精确统计。

## 主要改动文件

- `www/js/stats.js`：新增个人洞察统一快照和清晰度/变化计算。
- `www/js/ai.js`：改造聚合 payload、主动生成、同页渲染、本地数字优先和隐私边界。
- `www/index.html`：分析页改名为洞察并加入本地事实卡与 7/30 天选择器。
- `www/css/screens.css`：个人洞察样式、事实卡、共同出现、清晰度和移动端布局。
- `www/js/app.js`、`www/js/records.js`：切页、窗口切换和记录变更只刷新本地事实，不触发自动云端请求。
- `android/app/build.gradle`、`CHANGELOG.md`、`README.md`：版本升级到 v3.26 / versionCode 47。
- `debug/verify-133bc-browser.cjs`、`debug/verify-133bc-device.cjs`：新增浏览器和真机回归脚本。

## 验证结果

- `node --check www/js/stats.js`
- `node --check www/js/ai.js`
- `node --check www/js/app.js`
- `node --check www/js/records.js`
- 浏览器：`node debug/verify-133bc-browser.cjs` 通过；验证 7/30 天窗口、本地清晰度、无自动 fetch、自定义标签/备注不进入请求、本地比例覆盖恶意 AI 比例、来源标识和无控制台错误。
- 既有记录页回归：`node debug/verify-144-145-browser.cjs` 通过；15 项、多选/独占、内容自适应、滚动和固定底部操作区保持正常。
- APK：`npx cap sync android` 成功；`android\\gradlew.bat assembleDebug --no-daemon` 成功，产物 `android/app/build/outputs/apk/debug/app-debug.apk`。
- 真机：魅族 21（序列号 `461QYFDN226NF`）安装并启动成功；`node debug/verify-133bc-device.cjs` 通过。实测 v3.26、个人洞察标题、本地 7 天样本、无自动网络请求、30 天切换均正常，控制台错误 0。

## 待观察

- 数据清晰度阶段的阈值是产品启发式规则，不是医学或行为评分；继续观察真实记录量、覆盖天数和“说不清”比例后再调整。
- AI 提供商的具体网络策略仍由用户在设置中选择；本版本不自动切换提供商，也不上传自定义标签和备注。

