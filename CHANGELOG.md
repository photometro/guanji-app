# 观己 · 更新日志

> 「观己」是一个温和、非评判的习惯记录 App。数据仅存本地，AI 分析只接收聚合统计，不涉及单条记录。
> 更新日期：2026-08-17

---

## [3.17] 蓝色玻璃按钮材质优化（#83）

### 优化

- **蓝色来自表面，不再向外发光**：主按钮与「记录」按钮改为蓝色渐变表面、克制顶部反光和深蓝底缘，形成有厚度的蓝玻璃材质。
- **中性悬浮影**：移除紧贴按钮的蓝色外阴影，复用玻璃卡片同源的中性双层柔影；深色模式自动切换为黑色深度阴影。
- **按压更真实**：两个蓝色按钮按下时都会收起阴影，与已有缩放反馈共同表达“压回卡面”。

### 技术

- versionCode 38 / v3.17；前端资源缓存参数统一更新至 `v=107`

---

## [3.16] 汇总即时刷新 · 前台自动备份 · 设置层级统一（#127/#128/#129）

### 修复与优化

- **汇总页自定义项即时显示**（#127）：添加情绪或诱因后，当前汇总页立即刷新并自动选中新项；此前已选的多个选项保持不变。
- **打开 App 时自动备份**（#128）：完全替换后台切换触发；冷启动和从后台回到前台均等待 800ms 后检查，数据未变化不重复上传，重叠事件也只会执行一次。
- **「我的」页分组统一**（#129）：卡片表达模块；只有连续的跳转操作保留行间细分割线。单一“测试实况通知”操作去除孤立横线；数据保护的“修改口令”改为独立次级组。

### 技术

- versionCode 37 / v3.16；前端资源缓存参数统一更新至 `v=106`

---

## [3.15] 计时汇总交互状态修复（#126）

### 修复
- **汇总页情绪 / 诱因交互状态防护**（#126）：关闭全屏计时页时同步复位汇总子视图；隐藏的计时子视图不再参与触摸命中，避免页面切换或 OEM WebView 合成延迟造成残留状态影响后续交互。

### 技术
- versionCode 36 / v3.15；前端资源缓存参数统一更新至 `v=105`

---

## [3.14] 外观卡合并 · 按钮形态统一（#118/#123/#124/#125）

### 优化
- **外观卡与玻璃效果卡合并**（#125）：主题选择（跟随系统/浅色/深色）+ 玻璃效果开关 + 磨砂强度滑块 + 说明合为一卡；开关关闭时滑块保持常显；「实验性」标签移到玻璃效果开关标题旁；卡内间距收紧（14/14/10/10px）不拥挤
- **我的页按钮形态统一**（#124，方案 C）：单选=seg / 开关=switch / 操作=row-btn——外观、记录方式、AI 提供商三处胶囊按钮全部改为滑动滑块，与加密模式同款；AI 追问建议问题保留胶囊样式
- **危险操作卡恢复正常样式**（#118）：去掉特殊蓝色标记，与普通卡片视觉一致（独立成卡保留）
- **服务器配置弹窗地址框样式修复**（#123）：`type="url"` 输入框并入统一样式，地址/账号/密码三框一致

### 技术
- `refreshMeSegs`：切到「我的」页时一次重定位全部 4 组 seg 滑块
- versionCode 35 / v3.14

---

## [3.13] 数据卡交互打磨 · 加密滑动切换（#119/#120/#121/#122）

### 新增
- **加密模式滑动切换**（#121）：开启/关闭加密大按钮 → 「未加密 / 已加密」滑动滑块（与趋势卡曲线/热力图同款）

### 优化
- **关闭加密免口令**（#120）：切换加密/明文无需验证密钥（与「本机免解锁」同一信任边界），保留二次确认
- **自动备份开关收进弹窗**（#119）：卡片行改为普通入口行（与其余行交互一致），开关在弹窗内；入口副标题状态跟随（开启加密后可用/未配置服务器/已开启/已关闭）
- **行副标题间距**（#122）：2px → 8px，标题与小字不再粘连

### 技术
- versionCode 34 / v3.13

---

## [3.12] 数据管理三卡化（#117）

### 优化
- **数据管理整体重构**：数据区拆为三张卡——**数据保护**（加密状态 + 开关）/ **备份与恢复**（导出/恢复/备份文件/服务器备份/自动备份）/ **危险操作**（清除/演示，独立成卡）
- **恢复数据统一入口**：本机文件 / 服务器备份来源选择；导出数据副标题按模式自适应（CSV 文件 / 加密备份文件）
- **术语用户化**：合并/新增/覆盖等操作以通俗文案呈现；WebDAV 独立卡并入「服务器备份」一行
- 我的页功能顺序回归 v3.10 基线

### 技术
- versionCode 32 / v3.11；versionCode 33 / v3.12（#119-#122 交互打磨）

---

## [3.10] WebDAV 备份（P5）· 新用户默认经典样式

### 新增
- **WebDAV 备份**（#115）：加密模式专属的数据离机通道——配置你自己的 WebDAV 服务器（坚果云/群晖/Nextcloud），备份上传的是**密文**（只有你的口令能解开），——观己不提供任何云服务
  - 配置 + 连接测试（密码存系统安全存储，建议使用服务「应用专用密码」）
  - 手动备份：自动建 guanji/ 子目录，文件名带设备标识，多份历史全保留
  - **切后台自动备份**（默认关，数据变化才上传——指纹去重）
  - 从服务器恢复（合并/新增/覆盖三模式，覆盖前自动备份当前数据到本机）
  - 服务器备份管理（列表/删除）；清除数据时可连带删除服务器备份
  - 明文模式不可用（开启加密后可用——服务器备份是加密通道）

### 优化
- **新用户默认经典样式**（#116）：玻璃效果改为默认关闭，新用户先见经典界面（老用户设置保留，可随时开启）
- **隐私文案统一**：「数据默认仅存本地；WebDAV 备份由你自选——上传的是密文」
- 修改口令提示：历史备份（含服务器）仍需创建时的口令

### 技术
- 自建原生 WebDAV 桥（okhttp，支持任意 WebDAV 方法）——绕开 WebView CORS 限制
- 调试脚本移出仓库（debug/ 仅本地）

### 隐私承诺
- 记录默认只保存在这台设备上；WebDAV 备份由你自选，上传的是密文；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.9] 备份管理 · 导入逻辑自适 · 深色玻璃光带修复 · 磨砂强度可调 · 导入逻辑自适 · 深色玻璃光带修复 · 磨砂强度可调

### 新增
- **备份管理**（#110）：数据卡新增「备份管理」入口——下载目录中的备份/导出文件 App 内直接列出（🔒 加密包 / 📄 CSV 徽标）、行内二次确认删除、删除后自动刷新；不再需要手动到文件管理器清理
- **磨砂强度调节**（#113）：玻璃效果设置新增滑块（0-10px，默认 3，深浅共用）——日历/tab/记录面板的磨砂实时可调、持久保存

### 优化
- **导入弹窗逻辑自适**（#111）：口令框不再无条件出现——选中加密备份才显示、CSV 全程无需口令；明文态文案说明「加密备份需当初口令，CSV 无需口令」；备份列表区分类型
- **CSV 明文回导**（#111）：明文导出的 CSV 现在可以导回（按 日期+时间+时长 指纹合并去重，重复自动跳过）——导出/导入闭环打通
- **导出一致性**（#111）：加密态 0 条记录导出提示与明文一致；明文导入加密包后提示「当前以明文保存——建议开启加密」

### 修复
- **深色模式玻璃光带晕染**（#112）：深色底对比度高，大片灰白光带刺眼——深色下移除大面积光带（保留边框环 + 顶部高光，Material 深色语言）；浅色完全不变
- **导入 CSV 日期校验**：年份 4 位导致全部行被误判丢弃（已修复）
- **「显示大小」调小后 App 显示成原型模式**（#114）：真机铺满判定从「窄视口 + 触摸」改为仅按触摸设备判定——显示大小调小 / 分屏 / 全局缩放不再回退固定手机壳原型样式

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.8] 玻璃效果统一 · 数据卡加密 UI 完善 · 近 7 天口径文案 · 数据卡加密 UI 完善 · 近 7 天口径文案

### 新增
- **「玻璃效果」更名**（#108）：原「液态玻璃」更名「玻璃效果」——设置页开关与说明同步；内部机制不变，老用户设置保留
- **记录面板轻磨砂**（#109）：玻璃模式下记录面板统一 `blur(3px)`，与日历、tab 栏同款配方，三个浮层透明度观感一致

### 优化
- **加密态「关闭加密」升级顶部大按钮**（#106）：与「开启加密」同位置同色对称（蓝色）；状态区改为小圆点标识（未加密灰点 / 已加密绿点 + 绿色高亮文字）
- **数据卡间距**（#105）：加密大按钮与下方操作列表 14px 间距，明文/加密两态对称
- **文案口径统一**：「本周」→「近 7 天」（首页统计、分析按钮、AI 报告标签与提示词）——统计口径不变
- **清理 P1 拆分残留死文件**：移除旧版 `www/app.js`（101KB）与 `www/styles.css`（55.8KB），减小 APK 体积

### 修复
- **加密态「修改口令」分割线缺失**（#107）：secureActions 单按钮命中 last-child 规则致分割线被移除，已显式保留

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.7] 全链路本地加密 · 数据卡合并 · 备份导入

### 新增
- **本地加密**（#93）：开启后数据从写入那一刻起就是密文（AES-256-GCM + PBKDF2 口令派生 + DEK/KEK 信封，DEK 存系统安全存储）；导出/备份均为密文包，仅口令可解；支持修改口令 / 关闭加密
- **导出保存到下载目录**（#102）：导出文件通过系统存储 API 直接保存到公共「下载」目录（绕过部分手机分享面板没有标准「保存」的问题），toast 显示完整路径——明文 CSV 与加密备份均适用；分享面板取消不再误报错误
- **备份导入**（#104）：导入弹窗直接列出下载目录中的备份文件，点选即导入；也支持浏览其他位置
- **加密备份文件导出**（#95）：加密模式下导出为密文文件（明文模式保持 CSV）

### 优化
- **数据卡合并**（#96）：数据加密与数据管理合为一卡，导出按钮按模式自适应
- **toast 路径缩短**（#103）：导出提示多行展示，不再超出屏幕

### 修复
- **日期漂移 P0**（与 v3.6.1 hotfix 同源方案）：绝对日期键（dateKey）+ 旧记录 id 时间戳精确恢复 + 跨天自动检测
- **数据加密/导入弹窗输入框无样式**（#94）：密码框/文本框统一边框、圆角与聚焦高亮样式
- **数据卡分割线过多**（#97）：删除区块分隔线——4 个按钮由列表分隔线自然划分，不再「6 条线」密麻
- **导入「选择备份文件」点击无反应**（#99）：标签未关联隐藏的文件选择框，已补关联
- **口令机制说明**（#100）：导出提示「备份受你的加密口令保护」、导入提示「输入创建该备份时设置的口令」——消除「导出没做任何事却要口令」的困惑

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.6.1] 日期漂移 P0 修复（紧急补丁）

- **绝对日期键**（yyyy-mm-dd）：记录不再相对保存当天偏移，杜绝跨天漂移
- 启动/变更/跨天归一化 + 旧记录按 id 时间戳精确恢复
- 详情见 GitHub Release v3.6.1

---

## [3.6] 液态玻璃体系补全 · tab 栏动效与可读性

### 新增
- **tab 切换液体变形动效**（#81）：玻璃态选中胶囊改为单一滑块承载——切换时沿 0.55s 弹性曲线滑动，滑动途中胶囊挂 SVG 位移滤镜（feTurbulence + feDisplacementMap）做 0→20→0 的液体波动脉冲，每次形状随机；非玻璃态保持旧版滑块动画不变
- **次数趋势热力图视图**（#88）：趋势卡新增「曲线 / 热力图」双视图——网格按星期对齐（7 列）、蓝色系四档色阶（深浅模式各一套，深色更亮保证可读）+ 图例；后续多轮优化：固定行数网格（14 天恒 2 行 / 30 天恒 5 行）、格子平涂去微光、footer 统计与天数切换间距硬化
- **日历页轻磨砂**（#78）：玻璃态日历页加 `blur(3px)`（与 tab 栏同款配方），通透感保留、可读性提升
- **tab 栏轻磨砂**（#82）：玻璃态 tab 栏背景加 `blur(3px)` 轻磨砂（与日历页同款配方），图标/文字/胶囊可读性提升，通透感保留

### 优化
- **tab 选中胶囊定稿**（#76）：玻璃态选中指示改为全宽渐变玻璃胶囊——覆盖范围与旧版滑块一致（top/bottom 5px + left/right 4px + 圆角 22px），径向渐变 + 高光 + 单层投影，消除「两层阴影」观感
- **移除 tab 栏滚动收缩**（#77）：tab 栏恒 58px，切换 tab 不再出现高度变低残留（滚动收缩方案已确认移除）
- **日历明细可读性**（#79）：每日细项「N 分钟」蓝色加粗强调、情绪/诱因标签深色提权

### 修复
- **未来日期不可补记**（#80，双保险）：日历未来日期灰显禁用不可选；即使绕过 UI，补记未来日期也会被温和拦截（「未来的日期还没到哦，先记录今天吧」）
- **记录页两态切换高低差再修复**（#85）：步骤切换高度过渡再次校准，消除残余跳动

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录

---

## [3.5] 步骤切换动画修复 · 我的页精简

### 修复
- **记录面板步骤切换动画**：修复「弹出一部分 → 卡一下 → 显示完整」——下一步/小字直达/上一步切换时，面板高度改为正确的单调展开/收缩过渡（此前高度目标值计算错误导致反向收缩 + 收尾跳变，编辑记录不受影响）
- **浮层打开时返回键/侧滑不再直接退 App**（#87）：按返回键按浮层层级依次关闭（弹窗 → 日历 → 全屏计时 → 记录面板），全部关闭后才进入退出确认
- **矮视口状态栏显示不全**（#70）：矮视口下状态栏元素被裁切，已修复

### 优化
- **我的页布局精简**：隐私说明移至「数据管理」下方，随后并入「关于」卡——品牌区（logo/观己/版本号）下方直接是两条隐私说明（无分割线、无标题的页脚风格）
- **记录卡片悬浮形式**（#74）：首页最近记录卡片由贴底改为悬浮卡片（日历保持不悬浮）
- **记录卡片紧凑化**（#75）：记录卡片两态严格一致、整体缩小至与补记卡一致

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录

---

## [3.4] 实况通知增强 · 全屏计时汇总 · 键盘体验修复

### 新增
- **通知栏「结束并记录 / 取消」快捷按钮**：计时时无需打开 App——点「结束并记录」直接结束计时并进入记录汇总（时长已自动填好）；点「取消」终止计时；魅族胶囊展开态同步提供两个操作按钮
- **全屏计时汇总页**：点「结束记录」不跳页面，同屏切换为汇总视图——大数字时长、情绪/诱因可多选、看片自动由诱因推导、「存为记录 / 放弃」；**「继续计时」**误触结束可反悔，恢复后时长连续
- **状态条状标签（chip）**：Android 16 通知以「计时中」状态条形式呈现（更紧凑的锁屏胶囊）
- **AOD 息屏动态信息**：息屏卡片显示「已计时 X 分钟」，每分钟自动更新（配合前台服务保活，需在系统设置允许观己后台运行——设置页有引导文案）

### 修复
- **冷启动衔接**：App 被系统清理后点通知按钮，自动恢复计时 → 直达汇总/详情，不再闪现计时页
- **添加对话框键盘问题**（三轮修复）：输入预览与字数合并单行、键盘弹出时布局冻结 + 对话框跟随可见区——不再顶高预览框、不再页面拉扯、不再与键盘重合（面板与全屏汇总两套风格均修复）

### 体验
- **计时全屏页**：沉浸式主题蓝渐变 + 脉冲「进行中」徽标 + 96px 超大秒数（运动 App 风格）；面板 → 全屏有过渡动画衔接（尊重系统减弱动效设置）
- **前台服务保活**：息屏时分钟数持续刷新（部分手机厂商会冻结后台，亮屏自动纠正）

### 技术
- 进程被清理后点通知按钮也能正常衔接：自动恢复计时 → 完成操作（冷启动安全）
- 锁屏胶囊/通知走秒、杀进程恢复、权限降级链均不受影响

---

## [3.3] 全屏沉浸式计时页 · 记录流程语义修正

### 新增
- **全屏沉浸式计时页**（运动 App 风格）：点「开始记录」后整页进入计时——主题蓝渐变背景、脉冲「进行中」徽标、96px 超大等宽秒数、开始时刻、底部「结束记录」大按钮；后台实况通知照常，杀进程恢复直接回到全屏页
- 计时中按返回键 = 取消计时（温和提示），不会误退 App

### 修复
- **「补记」入口回归**：计时模式打开记录面板恢复「就现在/补记」切换——「就现在」= 计时，「补记」= 经典流程（计时开始后自动隐藏切换）
- **编辑记录不再有「就现在」选项**：编辑 = 修改这条记录的时间（只有补记语义），杜绝历史记录被意外覆盖成当前时刻

---

## [3.2] 魅族实况通知（锁屏胶囊）

### 新增
- **魅族（Flyme）实况通知适配**：计时时在魅族手机上显示**锁屏胶囊**（Flyme 私有胶囊机制，参考开源 Demo 逆向参数）——胶囊与通知内均为系统级计时秒数，切出应用后台精准跳动
- 双轨设计：魅族走私有胶囊，其他手机走 Android 标准实况通知/常驻通知，互不影响

### 技术
- 仅魅族设备附加胶囊参数（非魅族系统自动忽略，标准提升条件不被破坏）

---

## [3.1] 「就现在」计时记录 · 实况通知 · 经典模式保留

### 新增
- **「就现在」运动记录式计时**：点「开始记录」开始计时（大数字秒表），点「结束记录」自动进入详情并填入精准时长（按分钟取整）——时长不再靠事后估计
- **安卓实况通知**（Android 16+）：计时时切出应用，通知栏/锁屏持续显示秒数（系统级计时，后台精准跳动）；Android 12-15 显示普通常驻通知；通知权限关闭则纯 App 内计时照常
- **杀进程自动恢复**：计时中 App 被系统清理/意外关闭，重新打开自动恢复计时并弹出面板收尾（时长依然准确）
- **经典模式保留**：计时器态下方「不想计时？直接填写」直达原流程；设置页可切换「计时记录 / 快速记录」默认方式
- **实况通知测试**（设置页）：一键检测手机系统版本、通知权限与实况通知支持度，并发送 15 秒示例通知

### 交互
- 计时中关闭面板 = 取消本次计时（不产生记录），温和提示
- 计时中划掉通知 = 计时继续，下次打开 App 温和提示一次

---

## [3.0] 退场动画平滑化 · 焦点残留修复 · hover 粘滞修复

### 优化
- 记录/日历卡片退出动画全面平滑化：遮罩与卡片**统一曲线、同步退场**——下滑 + 微缩放 + 同步淡出（0.3s easeOutCubic），一体感十足
- 添加/删除对话框退场同步统一

### 修复
- 编辑面板**拖拽小横条退出**后，首页编辑按钮不再残留选中/高亮状态（面板打开时主动清除焦点）
- **选项取消选中不再残留黑字**（触摸设备 hover 粘滞问题，全 App 生效）：取消选中直接恢复灰色文字，无需再点一次

---

## [2.9] 退场动画顺滑化

### 优化
- 记录/日历卡片退出动画：改为**快启慢停**滑动曲线——开始立即滑动、结束自然减速，不再有「顿一下再走」的生硬感
- 添加/删除对话框关闭增加**退场动画**（淡出 + 轻微下移）——不再瞬间消失

---

## [2.8] 退场动画与自定义选项体验再修复

### 修复
- 退出动画不再「先降透明度卡一下」——退场只做下滑（卡片保持不透明，背景遮罩淡出），干净利落
- 键盘弹出时添加窗口不再整体偏上（对话框在键盘上方自然居中）
- 添加框下的「将添加」预览不再残留上次输入

### 优化
- 删除自定义选项改为 **chip 右上角 × 按钮**（仅自定义项显示）——点击即弹删除确认，不再依赖长按（长按会被手机系统菜单抢占，已移除）

---

## [2.7] 退场动画与自定义选项体验再修复

### 修复
- 记录页/日历页退出动画彻底消除「段落感」——退场时卡片转为不透明背景下滑，不再与下方内容叠影
- 长按自定义选项不再触发手机系统复制菜单（拦截系统长按行为）
- 删除确认卡片不再被记录面板遮住（层级修复：弹层置于面板之上）

### 优化
- 添加自定义选项更明确：按「完成」键不再直接添加（必须点「添加」按钮）；输入时实时预览「将添加：xxx」；键盘弹出时对话框自动上移，按钮不被遮挡

---

## [2.6] 动画与交互细节修复

### 修复
- 记录页/日历页点空白退出时不再有毛玻璃模糊感——退场动画期间临时关闭毛玻璃效果
- 深色模式下首页日历图标清晰可见（图标颜色改为跟随主题变量，修复深色下黑色图标不可读）

### 优化
- **编辑直达详情**：点「编辑」直接展开记录详情页（时间仍可调整），不再需要先点「下一步」
- **自定义选项可删除**：长按自定义的情绪/诱因选项 → 删除确认 → 移除（内置选项不可删，历史记录不受影响）
- **添加选项更明确**：输入时实时显示字数（n/6）

---

## [2.5] AI 配置体验修复

### 修复
- 配置 AI 时软键盘弹出，底部 tab 栏不再被顶到键盘上方——键盘弹出时 tab 栏自动隐藏，收起后恢复
- AI 提供商切换不再串密钥：DeepSeek / OpenAI / 自定义的 API 密钥**分别保存**，切换时自动回显各自的配置（旧版已保存的密钥自动迁移，不丢失）

---

## [2.4] 大字体适配修复

### 修复
- 系统字体放大（textZoom）时，「次数趋势」切换胶囊的「30 天」不再断行上下排布——胶囊文字永不换行、不被压缩（标题自动让位截断）
- 「我的」页 API 密钥「保存」按钮不再被输入框挤压——输入框可收缩让位，按钮完整并排
- 记录面板的时间分段控件同样受益（文字不再断行）

---

## [2.3] 小组件点击统一 · 记录编辑 · 报告自动刷新

### 新增
- **编辑记录**：首页「最近记录」和日历明细新增「编辑」按钮，打开记录面板自动预填（时间/情绪/诱因/时长/看片/备注），保存后原地更新，不会产生重复记录
- **报告自动刷新**：修改或删除记录后，AI 报告自动重新生成，保证时效性与可信度——在分析页时防抖 1.5 秒自动刷新（顶部轻提示，不打断阅读）；在其他页面时切回分析页自动刷新；刷新失败保留旧报告并温和提示
- 小组件点击行为统一：数据看板 / 连续记录 / 今日卡片 / 本周节奏点击只打开 App 首页，不再弹出记录面板

### 优化
- 快速记录小组件：移除「打开记录面板」按钮，整卡即一键快速记录（语义更纯粹）
- 次级按钮样式全局改进：文字加深 + 加边框，不再「灰得像禁用」

### 修复
- 分析页「重新生成分析」按钮：改为醒目主按钮样式；生成期间禁用防重复点击，失败后自动恢复（此前失败后会永久变灰）
- 编辑超长时长记录不再丢失数据（滑块上限 60 分钟，超出的时长以显示文本为准）
- 报告指纹升级为内容摘要：编辑情绪/时长等内容也能正确触发报告刷新

---

## [2.2] 桌面小组件全家桶（5 个 2x2）

### 新增
- **一键快速记录**：桌面「＋」按钮直接记录一条「就现在」，无需打开 App；顶部显示今日记录状态
- **数据看板**：本周次数大数字 + 较上周环比 + 今日次数 + 连续天数
- **本周节奏**：周一至周日 7 根柱状图，今日高亮，标签随真实星期对齐
- **连续进度**：连续天数 + 进度条（迈向 7/30 天里程碑），温和鼓励文案
- **今日卡片**：今日是否已记录 + 温和状态文案
- 数据同步机制：App 内任何记录变化自动同步到全部小组件

---

## [2.1] 弹层动效升级

### 新增
- 弹层新增小横杠，**拖拽可关闭**（超过阈值下滑关闭，未达阈值回弹）
- 弹层出场/退场动画：进入上滑、退出下滑淡出、背景淡入淡出，统一动效语言

### 优化
- 记录面板改为**自适应高度**：两步切换时高度平滑过渡，不再有大片空白
- AI 设置卡标签间距统一（Base URL / 模型 / 密钥）

### 修复
- 记录提醒时间清空后自动恢复默认 21:00，显示与存储保持一致

---

## [2.0] AI 多提供商 · 记录体验完善（9 项）

### 新增
- **AI 多提供商**：默认 DeepSeek，可选 OpenAI 或自定义（Base URL + 模型 + API 密钥），支持连接测试与分类报错
- **情绪/诱因自定义**：内置选项外可自主添加，本地持久化
- 分析页追问回复排版优化：加粗/列表/分段渲染，与报告卡片风格统一

### 优化
- 首页 14 天/30 天切换、记录面板时间分段：滑块跟踪动效
- 日历弹层点击空白处即可关闭
- 记录面板两步切换高度统一，不再上下跳动

### 修复
- 按钮点击后残留灰色边框（移除触屏焦点框，保留键盘可访问性样式）
- 补记日期时间为空/非法时显示「NaN」（改为回退当前时间 + 保存校验拦截）
- 旧版 API 密钥自动迁移到新配置

---

## [1.9] 修复

### 修复
- 时段分布环图周长公式错误（少乘 2），导致有数据的时段渲染成断裂短弧、环上出现大片空白弧位——修复后环完整连续

---

## [1.8] 环图多彩配色

### 优化
- 时段分布环图升级为多彩时间色（黄/橙/绿/青/蓝，深色模式自动提亮），中心数字与环段解耦

---

## [1.7] 问候语优化

### 优化
- 问候语拆层：标题固定一行时段问候（永不截断），AI 提醒句移到下方小字完整显示

---

## [1.6] 小组件 · 提醒 · 趋势（6 项）

### 新增
- **桌面小组件（2x2）**：点击直达记录面板
- **每日记录提醒**：可设置温和提醒时间（默认关闭）
- 趋势图 14 天/30 天切换 + 月度汇总
- 情绪-频率关联分析（AI 报告新增情绪观察）
- 温和正向反馈：连续记录 7/30 天里程碑（可在设置中关闭）

### 优化
- 问候语长度三保险（提示词限长 + 截断 + 单行省略）

### 修复
- 补记按钮重复绑定导致的行为异常

---

## [1.5] 修复

### 修复
- 日历按钮被长问候语挤压变形

---

## [1.4] 日历视图 · 深色模式 · 自动分析

### 新增
- **历史记录日历视图**：月历角标 + 选天明细 + 补记直达
- **分析页自动生成**：检测到新数据自动生成报告（指纹防重复 + 失败自动重试 3 次）
- **深色模式**：跟随系统 + 手动覆盖，防闪烁，图表颜色同步适配

---

## [1.3] 全面屏适配

### 优化
- 适配安卓全面屏（安全区适配、沉浸式状态栏）
- 底部 Tab 滑块圆角修复

---

## [1.2] 数据可视化升级

### 新增
- 时段分布改为「一天 100%」五段分段环图（深浅渐变 + 色点图例 + 动画）
- Tab 栏滑块跟踪样式（弹性过渡）

---

## [1.1] 首轮体验优化

### 新增
- 问候语动态化：按时段问候，并接入 AI 健康提醒（组合成一句话）
- Tab 栏美化

### 修复
- 两个体验细节 bug

---

## [1.0] 首个版本

- 记录面板：就现在 / 补记（日期时间、情绪、诱因、时长、看片、备注）
- 首页：今日概览、时段分布环图、趋势图、最近记录（可删除）
- AI 分析：聚合统计特征（频率/时段/情绪/诱因/连续天数）生成温和非评判报告，支持追问
- 数据仅存本地（持久化），可导出 CSV、恢复演示数据
- 温和的隐私设计：AI 只接收聚合数据，不涉及单条记录
