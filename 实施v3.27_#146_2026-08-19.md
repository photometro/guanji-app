# 观己 v3.27 · #146 实施记录

日期：2026-08-19  
版本：v3.27 / versionCode 48 / 前端资源缓存 v=117

对应改进清单编号：#146

## 实施范围

按已确认的推荐方案，将现有个人洞察页改为“基础洞察 + 可选 AI 增强”，不新增独立分析页，也不影响首页原有 30 天趋势。

## 已完成

- 个人洞察固定使用近 7 天，移除 7/30 天切换；近期变化仍比较前一个 7 天窗口。
- 恢复并固定六个模块：整体概览、时段分布、发生前状况、共同出现、近期变化、温和建议。
- AI 只由用户主动点击生成，统一一次请求后把文字观察嵌入对应模块；`#analysisResult` 不再承载独立报告。
- 本地次数、比例、时段和变化继续由 `buildInsightSnapshot({days: 7})` 提供，AI 不改写事实层数字。
- 增加 API Key 说明：基础洞察无需 AI；说明聚合数据范围、本机保存和服务商费用边界。
- 首页每日话语新增本地模板 `getLocalDailyTip(snapshot)`；无 API Key、无数据、断网时仍显示本地话语。
- 增加 `guanji_ai_daily_tip_enabled` 开关：新用户默认关闭；已有 API Key/旧每日话语缓存的用户首次进入设置页显示非阻塞迁移提示。
- 保留 `guanji_ai_config_v2`、`guanji_daily_tip`、报告指纹和旧记录字段；报告指纹加入固定窗口/模块布局版本。
- 保留 AI 聚合隐私边界，以及露骨、诊断、羞耻化和因果化输出拦截。

## 主要文件

- `www/index.html`
- `www/js/ai.js`
- `www/js/app.js`
- `www/js/ui-sheet.js`
- `www/css/screens.css`
- `android/app/build.gradle`
- `CHANGELOG.md`、`README.md`、`IMPROVEMENTS.md`

## 验证

- `node --check`：`ai.js`、`app.js`、`ui-sheet.js`、`stats.js` 通过。
- 浏览器：`debug/verify-146-browser.cjs` 通过；覆盖固定 7 天、六模块、无 Key 本地话语、无自动请求、AI 模块内嵌入、首页趋势保留和聚合请求脱敏。
- Android：`npx cap sync android` 通过；`assembleDebug --no-daemon` 构建成功。
- 真机：魅族 21（461QYFDN226NF）安装成功；`debug/verify-146-device.cjs` 通过，v3.27、六模块、固定 7 天、本地话语和无自动请求均正常。

## 隐私与兼容说明

AI 请求仅包含近 7 天聚合统计，不包含单条记录、备注、附件或自定义标签原文。旧版本记录、AI 配置、CSV/加密备份和每日缓存不删除；用户可随时关闭 AI 每日话语。

