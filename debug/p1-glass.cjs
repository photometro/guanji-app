(async () => { document.documentElement.classList.add('liquid-glass'); const out={}; const card=document.querySelector('.card'); out.cardBg=getComputedStyle(card).background.slice(0,40); out.tabbarBlur=getComputedStyle(document.querySelector('.tabbar')).backdropFilter; out.slideFilter=getComputedStyle(document.getElementById('tabSlide')).filter; out.slideRadius=getComputedStyle(document.getElementById('tabSlide')).borderRadius; out.glow=getComputedStyle(document.documentElement).getPropertyValue('--lg-glow-a').trim(); document.documentElement.setAttribute('data-theme','dark'); out.glowDark=getComputedStyle(document.documentElement).getPropertyValue('--lg-glow-a').trim(); document.documentElement.setAttribute('data-theme','light'); document.documentElement.classList.remove('liquid-glass'); return JSON.stringify(out); })();