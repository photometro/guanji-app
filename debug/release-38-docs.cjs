// 发布 v3.8：更新 README.md（功能/设计语言）+ CHANGELOG.md（新增 3.6.1/3.7/3.8 条目）
const fs = require('fs');
const R = 'C:/Users/43124/Desktop/test/guanji-app/README.md';
const C = 'C:/Users/43124/Desktop/test/guanji-app/CHANGELOG.md';

// ---------- README ----------
let r = fs.readFileSync(R, 'utf8');
let n = 0;
const repsR = [
  [
    '- **我的页**：AI 设置 / 记录方式 / 实况通知测试 / 数据管理（导出、清除、恢复演示数据）/ 隐私说明（数据仅存本地）',
    '- **本地加密**：数据从写入那一刻起就是密文（AES-256-GCM + 口令派生密钥），导出/备份均为密文包，仅口令可解；支持修改口令与关闭加密\n- **我的页**：AI 设置 / 记录方式 / 实况通知测试 / 数据卡（加密开关与口令 / 导出 / 备份导入（下载目录点选）/ 清除 / 恢复演示数据）/ 隐私说明（数据仅存本地）'
  ],
  [
    '- **液态玻璃体系（可开关）**：无磨砂通透配方——发丝暗线 / 渐变边框环 / 上缘高光 / 双层柔影；tab 栏与日历弹层按内容密度分层轻磨砂（`blur(3px)`）；选中指示为全宽渐变玻璃胶囊',
    '- **玻璃效果体系（可开关）**：无磨砂通透配方——发丝暗线 / 渐变边框环 / 上缘高光 / 双层柔影；tab 栏 / 日历弹层 / 记录面板按内容密度轻磨砂（`blur(3px)`）；选中指示为全宽渐变玻璃胶囊'
  ],
  [
    '**AI 只接收聚合特征**：发送给 AI 的只有统计摘要（本周次数、时段分布、情绪/诱因占比、连续天数等）',
    '**AI 只接收聚合特征**：发送给 AI 的只有统计摘要（近 7 天次数、时段分布、情绪/诱因占比、连续天数等）'
  ]
];
for (const [from, to] of repsR) {
  if (r.includes(from)) { r = r.replace(from, to); n++; }
  else console.log('README MISS:', from.slice(0, 40));
}
fs.writeFileSync(R, r, 'utf8');

// ---------- CHANGELOG ----------
let c = fs.readFileSync(C, 'utf8');
c = c.replace('> 更新日期：2026-08-09', '> 更新日期：2026-08-10');

const block = `## [3.8] 玻璃效果统一 · 数据卡加密 UI 完善 · 近 7 天口径文案

### 新增
- **「玻璃效果」更名**（#108）：原「液态玻璃」更名「玻璃效果」——设置页开关与说明同步；内部机制不变，老用户设置保留
- **记录面板轻磨砂**（#109）：玻璃模式下记录面板统一 \`blur(3px)\`，与日历、tab 栏同款配方，三个浮层透明度观感一致

### 优化
- **加密态「关闭加密」升级顶部大按钮**（#106）：与「开启加密」同位置同色对称（蓝色）；状态区改为小圆点标识（未加密灰点 / 已加密绿点 + 绿色高亮文字）
- **数据卡间距**（#105）：加密大按钮与下方操作列表 14px 间距，明文/加密两态对称
- **文案口径统一**：「本周」→「近 7 天」（首页统计、分析按钮、AI 报告标签与提示词）——统计口径不变
- **清理 P1 拆分残留死文件**：移除旧版 \`www/app.js\`（101KB）与 \`www/styles.css\`（55.8KB），减小 APK 体积

### 修复
- **加密态「修改口令」分割线缺失**（#107）：secureActions 单按钮命中 last-child 规则致分割线被移除，已显式保留

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.7] 全链路本地加密 · 数据卡合并 · 备份导入

### 新增
- **本地加密**（#93）：开启后数据从写入那一刻起就是密文（AES-256-GCM + PBKDF2 口令派生 + DEK/KEK 信封，DEK 存系统安全存储）；导出/备份均为密文包，仅口令可解；支持修改口令 / 关闭加密
- **备份导入**（#104）：导入弹窗直接列出下载目录中的备份文件，点选即导入；也支持浏览其他位置
- **加密备份文件导出**（#95）：加密模式下导出为密文文件（明文模式保持 CSV）

### 优化
- **数据卡合并**（#96）：数据加密与数据管理合为一卡，导出按钮按模式自适应
- **toast 路径缩短**（#103）：导出提示多行展示，不再超出屏幕

### 修复
- **日期漂移 P0**（与 v3.6.1 hotfix 同源方案）：绝对日期键（dateKey）+ 旧记录 id 时间戳精确恢复 + 跨天自动检测

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.6.1] 日期漂移 P0 修复（紧急补丁）

- **绝对日期键**（yyyy-mm-dd）：记录不再相对保存当天偏移，杜绝跨天漂移
- 启动/变更/跨天归一化 + 旧记录按 id 时间戳精确恢复
- 详情见 GitHub Release v3.6.1

---

## [3.6] 液态玻璃体系补全 · tab 栏动效与可读性`;

if (c.includes('## [3.6] 液态玻璃体系补全')) {
  c = c.replace('## [3.6] 液态玻璃体系补全', block);
  console.log('CHANGELOG inserted OK');
} else {
  console.log('CHANGELOG anchor MISS');
}
fs.writeFileSync(C, c, 'utf8');
console.log('README replaced:', n);
