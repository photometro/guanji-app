// 发布 v3.9：CHANGELOG 加 [3.9] 条目 + README 功能补全
const fs = require('fs');
const C = 'C:/Users/43124/Desktop/test/guanji-app/CHANGELOG.md';
const R = 'C:/Users/43124/Desktop/test/guanji-app/README.md';

// ---------- CHANGELOG ----------
let c = fs.readFileSync(C, 'utf8');
c = c.replace('> 更新日期：2026-08-10', '> 更新日期：2026-08-10');
const block = `## [3.9] 备份管理 · 导入逻辑自适 · 深色玻璃光带修复 · 磨砂强度可调

### 新增
- **备份管理**（#110）：数据卡新增「备份管理」入口——下载目录中的备份/导出文件 App 内直接列出（🔒 加密包 / 📄 CSV 徽标）、行内二次确认删除、删除后自动刷新；不再需要手动到文件管理器清理
- **磨砂强度调节**（#113）：玻璃效果设置新增滑块（0-10px，默认 3，深浅共用）——日历/tab/记录面板的磨砂实时可调、持久保存

### 优化
- **导入弹窗逻辑自适**（#111）：口令框不再无条件出现——选中加密备份才显示、CSV 全程无需口令；明文态文案说明「加密备份需当初口令，CSV 无需口令」；备份列表区分类型
- **CSV 明文回导**（#111）：明文导出的 CSV 现在可以导回（按 日期+时间+时长 指纹合并去重，重复自动跳过）——导出/导入闭环打通
- **导出一致性**（#111）：加密态 0 条记录导出提示与明文一致；明文导入加密包后提示「当前以明文保存——建议开启加密」

### 修复
- **深色模式玻璃光带晕染**（#112）：深色底对比度高，大片灰白光带刺眼——深色下移除大面积光带（保留边框环 + 顶部高光，Material 深色语言）；浅色完全不变
- **导入 CSV 日期校验**：年份 4 位导致全部行被误判丢弃（已修复）

### 隐私承诺不变
- 所有记录只保存在这台设备上，不上传云端；AI 分析仅上传聚合统计特征，不涉及单条记录。

---

## [3.8] 玻璃效果统一 · 数据卡加密 UI 完善 · 近 7 天口径文案`;

if (c.includes('## [3.8] 玻璃效果统一')) {
  c = c.replace('## [3.8] 玻璃效果统一', block);
  console.log('CHANGELOG [3.9] inserted OK');
} else {
  console.log('CHANGELOG anchor MISS');
}
fs.writeFileSync(C, c, 'utf8');

// ---------- README ----------
let r = fs.readFileSync(R, 'utf8');
let n = 0;
const reps = [
  // 数据卡行补备份管理
  [
    '- **我的页**：AI 设置 / 记录方式 / 实况通知测试 / 数据卡（加密开关与口令 / 导出 / 备份导入（下载目录点选）/ 清除 / 恢复演示数据）/ 隐私说明（数据仅存本地）',
    '- **我的页**：AI 设置 / 记录方式 / 实况通知测试 / 数据卡（加密开关与口令 / 导出 / 备份导入（下载目录点选）/ **备份管理（列表与删除）** / 清除 / 恢复演示数据）/ 隐私说明（数据仅存本地）'
  ],
  // 玻璃效果体系补磨砂滑块
  [
    '- **玻璃效果体系（可开关）**：无磨砂通透配方——发丝暗线 / 渐变边框环 / 上缘高光 / 双层柔影；tab 栏 / 日历弹层 / 记录面板按内容密度轻磨砂（`blur(3px)`）；选中指示为全宽渐变玻璃胶囊',
    '- **玻璃效果体系（可开关）**：无磨砂通透配方——发丝暗线 / 渐变边框环 / 上缘高光 / 双层柔影；tab 栏 / 日历弹层 / 记录面板轻磨砂，**磨砂强度可滑动调节**（0-10px 默认 3）；选中指示为全宽渐变玻璃胶囊'
  ]
];
for (const [from, to] of reps) {
  if (r.includes(from)) { r = r.replace(from, to); n++; }
  else console.log('README MISS:', from.slice(0, 30));
}
fs.writeFileSync(R, r, 'utf8');
console.log('README replaced:', n);
