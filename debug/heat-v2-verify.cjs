(async () => { const out={}; records=buildDemoRecords(); renderHome(); document.querySelector('#chartViewSeg .seg[data-view=heat]')?.click(); await new Promise(r=>setTimeout(r,300)); out.labels=[...document.querySelectorAll('.heat-labels span')].map(s=>s.textContent).join(''); out.today=!!document.querySelector('.heat-cell.today'); out.msOneLine=Math.round(document.getElementById('monthSummary').getBoundingClientRect().height)<=18; out.heatCount=document.querySelectorAll('.heat-grid .heat-cell').length; return JSON.stringify(out); })();