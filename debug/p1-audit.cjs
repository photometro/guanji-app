// P1 状态审计：修正 IMPROVEMENTS.md 过时标记 + 删除 #42 重复
const fs = require('fs');
const p = 'IMPROVEMENTS.md';
let s = fs.readFileSync(p, 'utf8');
const lines = s.split('\n');
const out = [];
let skip = false; // 跳过重复 #42 区块

const fixes = {
  '## 20. 日历视图支持点空白退出 🆕 待实施': '## 20. 日历视图支持点空白退出 ✅ 已实施（点空白关闭面板，日历优先）',
  '## 21. 首页 14/30 天切换增加滑块动效 🆕 待实施': '## 21. 首页 14/30 天切换增加滑块动效 ✅ 已实施（通用 seg 滑块动效）',
  '## 22. 记录面板「就现在/补记」分段加滑块动效 🆕 待实施': '## 22. 记录面板「就现在/补记」分段加滑块动效 ✅ 已实施（通用 seg 滑块动效）',
  '## 23. 记录面板两步切换高度跳动修复 🆕 待实施': '## 23. 记录面板两步切换高度跳动修复 ✅ 已实施（v3.4 补丁，见 #66）',
  '## 24. 情绪/诱因支持自定义添加 🆕 待实施': '## 24. 情绪/诱因支持自定义添加 ✅ 已实施（v2.9，见 #47）',
  '## 25. 【Bug】按钮点击后灰色边框残留 🆕 待实施（已定位根因 ✅）': '## 25. 【Bug】按钮点击后灰色边框残留 ✅ 已修复（关闭 UA focus outline）',
  '## 26. 【Bug】补记模式日期时间显示 NaN 🆕 待实施（已定位根因 ✅）': '## 26. 【Bug】补记模式日期时间显示 NaN ✅ 已修复（空/非法日期拦截保存）',
  '## 27. AI 提供商可配置（多提供商 + 连接测试） 🆕 待实施': '## 27. AI 提供商可配置（多提供商 + 连接测试） ✅ 已实施（v2.5，见 #43）',
  '## 28. 记录面板空旷问题修复（自适应 + 高度过渡动画） 🆕 待实施（方案已确认 2）': '## 28. 记录面板空旷问题修复（自适应 + 高度过渡动画） ✅ 已实施（v3.4 补丁，见 #66）',
  '## 29. 弹层小横杠拖拽关闭 + 出场/退场动画 🆕 待实施': '## 29. 弹层小横杠拖拽关闭 + 出场/退场动画 ✅ 已实施（通用弹层动画+拖拽）',
  '## 32. 小组件「一键快速记录」+ 信息增强 🆕 待实施': '## 32. 小组件「一键快速记录」+ 信息增强 ✅ 已实施（v2.1-2.2）',
  '## 33. 新增 2x2 数据看板小组件 🆕 待实施': '## 33. 新增 2x2 数据看板小组件 ✅ 已实施（v2.1-2.2）',
  '## 34. 本周节奏小组件（2x2） 🆕 待实施': '## 34. 本周节奏小组件（2x2） ✅ 已实施（v2.1-2.2）',
  '## 35. 连续记录进度小组件（2x2） 🆕 待实施': '## 35. 连续记录进度小组件（2x2） ✅ 已实施（v2.1-2.2）',
  '## 36. 今日卡片小组件（2x2） 🆕 待实施': '## 36. 今日卡片小组件（2x2） ✅ 已实施（v2.1-2.2）',
  '## 53. 排版设计规范（标题字体/字号/间距统一文档化） 🆕 待应用': '## 53. 排版设计规范（标题字体/字号/间距统一文档化） ✅ 已应用（规范文档化 + styles.css 全面核对）',
};

let fixed = 0;
let dupDeleted = false;
for (let i = 0; i < lines.length; i++) {
  const line = lines[i];
  // 删除重复 #42（1637 行区块：标「已定位待实施」的）
  if (line.startsWith('## 42.') && line.includes('已定位')) {
    skip = true;
    continue;
  }
  if (skip) {
    if (line.startsWith('## ')) skip = false; // 到达下一标题，恢复
    else continue;
  }
  if (fixes[line]) {
    out.push(fixes[line]);
    fixed++;
  } else {
    out.push(line);
  }
}
fs.writeFileSync(p, out.join('\n'), 'utf8');
console.log('修正标题数:', fixed, ' 删除重复 #42 区块:', dupDeleted ? '是' : '否（按标题匹配处理）');
// 校验没有遗漏
const remaining = out.filter((l) => l.startsWith('## ') && l.includes('🆕'));
console.log('剩余 🆕 待办条目:');
remaining.forEach((l) => console.log('  ' + l.slice(0, 85)));
