(async () => { function measure(){ const s=document.querySelector('.sheet'); const r=s.getBoundingClientRect(); return Math.round(r.height*10)/10; } openSheet('now'); const now=measure(); closeSheet(); await new Promise(r=>setTimeout(r,450)); openSheet('backfill'); const backfill=measure(); closeSheet(); await new Promise(r=>setTimeout(r,450)); openSheet('now'); const now2=measure(); closeSheet(); return JSON.stringify({now,backfill,now2,diff:Math.round((now-backfill)*10)/10}); })();