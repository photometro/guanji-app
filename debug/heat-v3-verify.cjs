(async () => { const out={}; records=buildDemoRecords(); renderHome(); document.querySelector('#chartViewSeg .seg[data-view=heat]')?.click(); await new Promise(r=>setTimeout(r,300)); const cell=document.querySelector('.heat-grid .heat-cell'); const cs=getComputedStyle(cell); out.cellSize=cs.width+'x'+cs.height; out.today=!!document.querySelector('.heat-cell.today'); out.msOneLine=Math.round(document.getElementById('monthSummary').getBoundingClientRect().height)<=18; return JSON.stringify(out); })();