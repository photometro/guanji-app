(async () => { const out={}; function geo(){ const s=document.querySelector('.sheet'); const r=s.getBoundingClientRect(); return { top:Math.round(r.top), h:Math.round(r.height), left:Math.round(r.left), w:Math.round(r.width) }; } openSheet('now'); document.documentElement.classList.add('liquid-glass'); await new Promise(r=>setTimeout(r,600)); out.glassNow=geo(); document.documentElement.classList.remove('liquid-glass'); await new Promise(r=>setTimeout(r,600)); out.plainNow=geo(); closeSheet(); return JSON.stringify(out); })();